#include "main.h"
/**
* _isupper - checks for uppercase
* @c: The character being tested
* Return: Always 0
*/

int _isupper(int c)
{
	int c = 65;

	if (c < 90)

		return (1);

	else

		return (0);
}

