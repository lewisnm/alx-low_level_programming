#include <stdio.h>

/**
* main - Entry point
* Description - displays all numbers base 10 single numbers
* Return: (0) success
*/
int main(void)
{

	for (int i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}

	return (0);
}
