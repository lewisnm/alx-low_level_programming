#include <stdio.h>

/**
* main - Entry point
* Description - displays all numbers base 10 single numbers
* Return: (0) success
*/
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}

	return (0);
}
