#include "main.h"
#include <stdio.h>

/**
 * reverse_array - this reverses an array
 *
 * @a: the array itself
 * @n: elements required
 *
 * Return: void
 */

void reverse_array(int *a, int n)
{
	int c = 0;

	while (s[c] != '\0')
	{
		c++;
	}

	for (c -= 1; c >= 0; c--)
	{
		_putchar(s[c]);
	}

	_putchar('\n');
}
